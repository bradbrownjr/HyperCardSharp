@AGENTS.md

## Claude-Specific Notes

- Prefer the Read tool before editing any file — understand existing code before modifying it.
- When reverse engineering stack format blocks, document findings inline with byte offsets and field sizes.
- Keep responses concise. Lead with the answer or action, not the reasoning.
- When uncertain about a HyperTalk behavior or stack format detail, say so explicitly rather than guessing.
